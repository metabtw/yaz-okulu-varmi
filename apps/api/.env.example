# ============================================================
# Yaz Okulu Var mı? - Backend Ortam Değişkenleri
# Bu dosyayı .env olarak kopyalayın ve değerleri doldurun.
# ============================================================

# Veritabanı bağlantısı (Docker Compose ile uyumlu)
DATABASE_URL="postgresql://yazokulu:yazokulu123@localhost:5432/yazokulu_db?schema=public"

# JWT Gizli Anahtarı - Üretim ortamında güçlü bir anahtar kullanın
JWT_SECRET="super-secret-jwt-key-degistir-beni"

# Sunucu portu
PORT=4000

# Frontend URL (CORS için)
FRONTEND_URL="http://localhost:3000"

# Redis bağlantısı (ileride cache için)
REDIS_URL="redis://localhost:6379"

# Fuzzy Search (pg_trgm Extension)
# Migration başarılıysa true, değilse fallback token-based search kullanılır
ENABLE_TRGM=true
